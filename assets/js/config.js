/* ──────────────────────────────────────────────────────────────────────────
   config.js — the ONE place to edit human-facing contact details.
   Change a value here and it updates everywhere on the site at page load.
   (SEO-critical values — canonical URLs, OG tags, sitemap, schema — are
   hardcoded in each page's HTML and are NOT controlled from this file.)
   ────────────────────────────────────────────────────────────────────────── */
window.SITE_CONFIG = {
  // Phone shown to visitors (any format you like)
  phoneDisplay: "(+66) 065-156-1561",

  // WhatsApp number, digits only, country code first (used for wa.me + tel:)
  whatsapp: "66651561561",

  // LINE id (used for line.me/ti/p/~<id>)
  lineId: "thailandeliteinfo",

  // Contact email
  email: "info@thailandelite.net",

  // Who clients reach (shown near contact points)
  contactName: "Patcharaphol “Pat” Nabhaboriraks"
};
